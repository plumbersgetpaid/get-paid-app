// This component has been replaced by NotesSection.jsx, which owns the
// whole notes list (fetch, add, delete) as one self-contained piece rather
// than splitting the form from the list across server/client boundaries.
// Left as an empty, unused file rather than deleted, since deletion isn't
// available through this workflow - safe to ignore, nothing imports it.
export default function AddNoteForm() {
  return null;
}
